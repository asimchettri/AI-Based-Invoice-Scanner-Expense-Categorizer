##trying to make tessarac work on render
services:
  - type: web
    name: ai-invoice-api-service
    env: python
    buildCommand: |
      apt-get update
      apt-get install -y tesseract-ocr
      pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
